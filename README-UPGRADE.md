# Quantum Shadow Liquid Motion Upgrade

Drop these files into the existing `antoniojmiranda82-cyberdev-quantum-shadow-technologies-chess` repository, preserving the folder paths.

Files:
- `app/layout.tsx`
- `app/liquid-motion.css`
- `components/pieces/PieceExperience.tsx`
- `components/pieces/PieceNarrative.tsx`
- `components/three/PieceScene.tsx`

Important: do not copy the uploaded `package-lock.json` into the chess repo. It belongs to an older Vite project. Keep the lockfile already generated by the working Next.js deployment, or regenerate it from the chess repo's `package.json`.

After replacement:
1. Save all files.
2. In GitHub Desktop, commit with `Add cinematic liquid motion`.
3. Push origin.
4. Vercel will redeploy automatically.
